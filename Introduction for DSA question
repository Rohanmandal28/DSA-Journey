Write a Program to generate the Armstrong numbers from N to M. 
#include <stdio.h>
#include <math.h>

int main() {
    int N, M, num, temp, digit, sum, digits;

    printf("Enter N and M: ");
    scanf("%d %d", &N, &M);

    printf("Armstrong numbers between %d and %d are:\n", N, M);

    for (num = N; num <= M; num++) {
        temp = num;
        sum = 0;
        digits = 0;

        // Count digits
        while (temp != 0) {
            temp /= 10;
            digits++;
        }

        temp = num;

        // Calculate Armstrong sum
        while (temp != 0) {
            digit = temp % 10;
            sum += pow(digit, digits);
            temp /= 10;
        }

        if (sum == num)
            printf("%d ", num);
    }

    return 0;
}
